// Shared identifiers for the connector-less request bucket, used by both
// the public lead-intake endpoint (app/api/leads) and the WhatsApp
// request-capture flow in the webhook.
export const UNASSIGNED_CONNECTOR_CODE = 'DIRECT-UNASSIGNED'
export const UNASSIGNED_VENDOR_SLUG = 'unassigned'
